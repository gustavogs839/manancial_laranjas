{
  "version": 2,
  "builds": [
    {
      "src": "react-app/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ]
  ,
  "routes": [
    { "src": "^/$", "dest": "/react-app/" },
    { "src": "^/(.*)", "dest": "/react-app/$1" }
  ]
}
